# AC→test-traceability — Story 17.2 (Bootstrap-wachtrij: prioritering en beheer)

Elk AC → de test(s) die het gedrag afdwingen. Alle tests geautomatiseerd; geen waivers.

| AC | Gedrag | Dekkende test(s) |
|----|--------|------------------|
| **AC1** Initiële vulling op declaratiefrequentie (idempotent seed, `--dry-run`, alle klassen zonder actieve referenties, universum-frequentie, FR-15-verrijking niet-overschrijvend, niet-visueel→uitgesloten, status-guard) | `apps/api/src/__tests__/scripts/seed-bootstrap-queue.test.ts` — `planSeed` (rangschikking op frequentie aflopend; onbekende staart→freq 0; niet-visueel→uitgesloten; idempotentie=skip; verouderde freq→enrich; uitgesloten→skip nooit terug; run-status blijft) + `buildUniverse` (bekende ∪ bestaande minus actieve klassen). Frequentiebron: `bootstrap-frequency.ts` (§3-tabel + `FREQUENCY_SOURCE`). |
| **AC2** Paneel + beheer-endpoint (declaratiefrequentie + status gesorteerd; override/uitsluiten/toevoegen; elke mutatie gelogd; uitgesloten nooit verwerkt) | Service-unit: `apps/api/src/__tests__/services/flywheel-bootstrap-queue.test.ts` — `effectiveQueueOrder` (override>frequentie>deterministisch, muteert niet), `getBootstrapQueue` (effectieve volgorde), mutaties (`setPriorityOverride`/`setExcluded`/`addClass` + audit-log via `threshold_changes` met userId+reason; 404 onbekend; add idempotent; lege code→error). Endpoint-integratie: `apps/api/src/__tests__/api/flywheel-bootstrap-queue.routes.test.ts` — GET/PATCH/POST met ADMIN-only (403 USER), override zetten/wissen, excluded togglen, add, enqueue, 400-validatie, 404. Contract-regressie 17.1×17.2 (uitgesloten nooit in run-selectie): `apps/api/src/__tests__/services/flywheel-bootstrap-run.test.ts` "verwerkt nooit een uitgesloten klasse (excluded=false in de wachtrij-query)". Web: `apps/web/src/components/flywheel/BootstrapQueuePanel.test.tsx` — render frequentie+status in API-volgorde, statusbadges, lege staat (UX-DR8), uitsluiten/toevoegen/enqueue via service. |
| **AC3** Nieuw geactiveerde klasse (read-side: status `gevuld` + ≥1 actieve `ReferenceLogo` `source=flywheel-promotion` met kandidaat-herkomst `bootstrap`) | Service: `flywheel-bootstrap-queue.test.ts` — `determineNewlyActivatedCodes` (lege invoer→geen query; wél/niet actieve bootstrap-promotie; query-contract origin=bootstrap + referenceLogo{active,source}) + `getBootstrapQueue` (newlyActivated-markering). Web: `BootstrapQueuePanel.test.tsx` — melding getoond / niet-getoond. |
| **AC4** Doorklik naar bewijs (batch-detail 15.3, herkomst `bootstrap`) | Web: `BootstrapQueuePanel.test.tsx` — "navigeert bij doorklik naar de batch-detail op het batch-id (AC4)" (`navigate('/flywheel/batches/{activatedBatchId}')`) + "zonder batch-id → doorklik uitgeschakeld". Service: `flywheel-bootstrap-queue.test.ts` — `determineNewlyActivatedCodes` levert het promotie-batch-id (nieuwste batch wint), `getBootstrapQueue` draagt `activatedBatchId`. **Correctie (adversarial review H1):** de doorklik gaat naar het promotie-batch-id (UUID) dat de batch-detail-route (15.3, `batch-detail.ts` resolvet op batch-UUID) verwacht — NIET naar de T3777-code (dat gaf een 404). De evidence-weergave zelf is het bestaande 15.3-scherm (toont `origin`), geen nieuwe weergave. |
| **AC5** Tests | Alle bovenstaande. Unit (sorteerlogica, seed-idempotentie, status-guard, nieuw-geactiveerde-bepaling), integratie (endpoint-CRUD + auth + logging-asserts + GET-sortering), web (paneel/sortering/badges/doorklik/lege staat/NL-i18next), contract-regressie 17.1×17.2. E2E buiten de stable-subset-gate (niet toegevoegd). |

## Niet-code-AC's / afbakening
- Geen migratie (tabel is 16.2-eigendom; alle kolommen aanwezig — geverifieerd in `schema.prisma:838-858`). Seed raakt alleen `wachtend`/`uitgesloten` + `declarationFrequency`.
- Frequentiebron §3 (2026-06-09-meting) als canonieke tabel; staart→0 (gedocumenteerde keuze). Verse universum-hermeting is read-only/off-peak toegestaan zonder governance-akkoord (buiten deze story).
