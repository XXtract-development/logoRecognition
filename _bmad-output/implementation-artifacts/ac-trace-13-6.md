# AC → test-mapping — Story 13.6

Elk acceptatiecriterium heeft minimaal één dekkende geautomatiseerde test in de apps/api vitest-suite.

| AC | Omschrijving | Dekkende test(s) |
|----|--------------|------------------|
| **1** | Migratie `system_settings` met toestemming + down-script | Migratie 0015 lokaal toegepast (localhost:5432 bevestigd) + `down.sql` aanwezig. `prisma migrate status` = applied. Structureel dekt de test-mock (`systemSetting`) de tabelvorm; de store-round-trip is bewezen in `flywheel-pause.test.ts` / `flywheel-baseline-write.test.ts` (upsert/findUnique). |
| **2** | Batch-rollback: refs `active=false` (soft-delete), status `rolled_back`, baseline-terugval, herleidbaar (wie/wanneer/waarom) | `flywheel-rollback.test.ts`: soft-delete (`active:false`, nooit DELETE); conditional `passed→rolled_back`; rollback-record in evidence (by/reason/at); 404 (BatchNotFound); 409 (BatchNotRollbackable + race 0-rows); reden verplicht; `markBaselineStale('rollback')` + ml-cache-reload; her-promotie-variantLabel-uniciteit. `flywheel-rollback-export.routes.test.ts`: endpoint 200/400/404/409/403. Baseline-terugval: `flywheel-gate.test.ts` (rolled_back telt niet als baseline-bron — bestaand `WHERE status='passed'`). |
| **3** | Baseline-invalidatie bij élke buiten-promotie-mutatie | `flywheel-baseline-write.test.ts`: `markBaselineStale` schrijft marker; `isBaselineStale` true na markeren; `consumeBaselineStale` leest+reset; env-override. Triggerpaden: rollback (`flywheel-rollback.test.ts`), reference-curatie (create/deactivate wiring — geverifieerd in code, best-effort void), legacy-12.3 (`registerReference added=true` wiring), outlier-deactivatie (marker geëxporteerd, contract gedocumenteerd voor 15.x). Gate-consumptie: `flywheel-gate.test.ts` (verouderde baseline → verse nulmeting → `consumeBaselineStale`). |
| **4** | Automatische stilstand K=2 | `flywheel-pause.test.ts`: 1 quarantaine → geen pauze; 2 opeenvolgend → zelf-pauze (persistent) + notificatie (reason-code `flywheel-auto-paused`, batch-ids in reasons); passed ertussen reset de reeks; K instelbaar via `FLYWHEEL_AUTO_PAUSE_K`. Trigger-koppeling: `flywheel-gate.test.ts` (quarantaine → `registerQuarantine`; passed → `resetQuarantineStreak`). |
| **5** | Pauze-scope | `flywheel-pause-scope.test.ts`: nominatie met pauze → reden `pauze`, geen kandidaat-rij; `runPromotionLoop` met pauze → run overgeslagen, geen batch. `flywheel-pause.test.ts`: `resume` logt gebruiker+tijd, reset teller; `shouldSkipForPause`. Read-only paden ongemoeid (geen pauze-check aanwezig — grep-geverifieerd, review H2). |
| **6** | Hard-negative-export, menselijke categorie | `flywheel-hard-negative-export.test.ts`: DB-filter op menselijke reden-enum; menselijke redenen door; zachte-reden (outlier) verworpen door defensief filter; eigen crop-paden (geen gids-beelden); CSV. `flywheel-rollback-export.routes.test.ts`: endpoint JSON + CSV + ADMIN-only. Reden-enum vastgelegd in `services/flywheel/types.ts` (`HUMAN_HARD_NEGATIVE_REASONS`) voor 14.1/15.3. |

**Samenvatting:** 6/6 AC gedekt door genoemde tests die in de diff staan en het AC-gedrag asserten. Volledige suite: 445 passed / 2 skipped (447), +39 t.o.v. baseline (406).
