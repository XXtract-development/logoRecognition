# Handover — 2026-06-09 (Epic 12: brede keurmerk-dekking)

## Samenvatting / waar we staan

Epic 12 (brede keurmerk-dekking uit de GS1 Packaging Label Guide) is gestart en ver gevorderd:

- **Story 12.1 (proof-slice) = DONE** — mechanisme end-to-end gebouwd, gedeployed en gevalideerd op ACC.
  Het systeem ging van **5 → 10 keurmerk-klassen**. Onderweg een echte bug gevonden + gefixt (RECYCLABLE).
  Evidence-based verdict: single-reference + ImageNet-embedding schiet tekort (9% precisie) → 12.2 nodig.
- **Story 12.2 (architectuur-spike) = DONE** — alle stappen (2–5 / AC1–AC4) gemeten op ACC met de echte
  productie-componenten. Verdict: **GO op de architectuur, GATED op embedding-fine-tuning.**
  Volledig rapport + beslisdocument: `12-2-spike-results-and-decision.md`.
  Kernuitkomsten:
  - **AC1 kosten-ontkoppeling BEWEZEN:** region-proposer ~1,18 s/beeld **constant** bij 5→894 klassen vs
    template-match lineair (23,6 s @5 → 70 min @894); ~800× sneller bij N=200. Kost = propose + regio's×embed
    (klasse-onafhankelijk) + verwaarloosbare ANN-term.
  - **AC2 (gedecomponeerd) — embedding is HET knelpunt:** perfecte-crop classify-accept maar **17 %**
    (echte marks op cosine 0,62–0,78, ónder de 0,75-poort), proposer-recall 45 %@IoU0,5 (69 % "surfaced"),
    end-to-end 9,3 % (≙ AC5). **Precisie van geaccepteerde = 100 %, 0 fout-accepts** → faalpatroon is
    *onthouding*, niet verwarring.
  - **AC3 open-set PASS:** 16/16 VALS-records → UNKNOWN; niet-logo-FP-rate 0,24 % (13/5424).
  - **AC4 verdict:** optie C bevestigd; **embedding-fine-tuning = VEREIST** (gated-beslissing valt op JA met
    data) → grootste precisie-hefboom, aparte vervolgstory (metric-learning op keurmerk-crops + 8.7-synthese).
    Productie-build GATED hierop; bouw het twee-traps-pad als A/B-migratie náást template-match.
  - Meetharnas: `apps/ml-service/scripts/spike_pipeline_eval.py` (ac1 / goldset). Artefacten in
    `_bmad-output/spikes/12-2-region-proposer/` (ac1-latency.json, ac2-ac3-results.json, diag_*.png).

Branch: `acc`. Live ACC image: `16c8a5f`. Laatste commit (spike, lokaal→push): `f356b76`.

## Belangrijkste bevindingen (met bronbestanden)

1. **Gold-set bestaande 5 klassen:** ≥0,7 = **100%**, ≥0,5 = 82% precisie op echt artwork.
   → `tests/validation/gold-set-oogstrun.json`, labels `…/labels-oogstrun.json` (let op: dezelfde bestandsnaam
   werd hergebruikt voor latere labelrondes — de gold-set-json is de bevroren bron).
2. **Scope = 6 GS1-velden, ~894 T3777-codes** (vs 5 gemodelleerd); GS1 Label Guide bevat per code het officiële
   logo (945 codes / 1026 embedded images). → `tests/validation/keurmerk-declaratie-frequentie.md`.
   Bronbestanden van gebruiker in `~/Downloads/`: `Packaging_label_guide_January2026_3_1_35.xlsx` (logo's),
   `benelux-fmcg-data-model-31353-nederlands.xlsx` (codelijsten).
3. **AC5 proof-slice-validatie:** na alle fixes **9% precisie** op de 5 nieuwe klassen → 12.2 gerechtvaardigd
   met data. → `tests/validation/ac5-proof-slice-validatie.md` (+ labels).
4. **RECYCLABLE@1,00-bug:** transparant logo → `convert('RGB')` → zwart → degeneraat embedding (norm 3,87 vs
   gezond ~9,3) → cosine 1,00 op alles. Fix: transparantie op WIT platslaan in extractie. Geverifieerd
   (norm→10,59, 681 high-conf FP→0). Commit `751596a`.
5. **12.2 region-proposer-POC:** 60 regio's/265ms, class-agnostisch, vond Nutri-Score + pictogrammen.
   → `apps/ml-service/scripts/spike_region_proposer.py`, viz `_bmad-output/spikes/12-2-region-proposer/`.

## ACC live-staat (geverifieerd)

- **10 actieve `reference_logos`** (5 oud + 5 proof-slice met `field_type`), 10 `reference_embeddings`.
- Proof-slice codes: RECYCLABLE_GENERAL_CLAIM, TRIMAN, BETER_LEVEN_1_STER, EUROPEAN_V_LABEL_VEGETARIAN,
  MARINE_STEWARDSHIP_COUNCIL_LABEL (alle `field_type=ACCREDITATION`, met de WITTE-fix-logo's).
- **DB-migratie toegepast:** `field_type VARCHAR(30) DEFAULT 'ACCREDITATION'` op `reference_logos`
  (via `ssh cherry` → `sudo -u postgres psql -d logo_recognition`; de app-rol `logorecognition` is GEEN owner).
- **Nieuwe ML-endpoint live:** `POST /ml/artwork/rebuild-reference-embeddings` (rebuild zonder ML-herstart).
- ML draait met **4 uvicorn-workers** (compose `--workers 4`) + `DETECTION_CONCURRENCY=4`.
- ~2000+ artwork-beelden staan geïmporteerd in `artwork_imports` (van eerdere prod-media batches).

## Herbruikbare mechanismen (operationeel)

- **Prod-media ingest** (artwork → MinIO + `artwork_import` + detect): de mediaserver-discovery indexeert maar
  ~54 GTINs; de echte bron is **prod MySQL `xxtractdbmedia.media`** (read-only via mysql-dba agent), bestanden op
  de ACC-NAS `/mnt/storagebox-home/acc/TXmedia` + `sourceLocation`. Zie `[[project_acc_bulk_data_gaps]]`.
- **Seed-runner:** `apps/api/scripts/seed-reference-logos-from-guide.js` (in-container; upload + upsert + rebuild).
- **Extractie:** `apps/api/scripts/extract_gs1_label_guide.py` (anker→code, WIT-flatten, manifest).
- **Labelronde-tooling:** crops rechtstreeks uit MinIO halen (het `/crop`-API-endpoint is onbetrouwbaar onder
  rapid-fire) via `mc cat loc/training-images/{cropPath}`; HTML-builder `/tmp/build_labelronde.py`.
- **Token minten:** `docker exec <app> node -e "jwt.sign({role:'ADMIN',...}, process.env.JWT_SECRET)"`.
- **Containers:** namen wijzigen per deploy — altijd opnieuw resolven met `docker ps | grep <svc>-qsookwow`.

## Volgende stappen (12.2-spike DONE → vervolg)

De 12.2-spike (AC1–AC4) is afgerond; zie hierboven + `12-2-spike-results-and-decision.md`. Vervolg, in
volgorde van precisie-hefboom:

1. **Vervolgstory "embedding-fine-tuning" (HOOGSTE prioriteit, gated-JA uit AC4):** metric-learning
   (triplet/ArcFace) op keurmerk-crops — bron: gold-set-loop + Story 8.7-synthese. Doel: echte marks boven de
   0,75-poort tillen zonder precisie te slopen (nu 100 % precisie, 17 % recall → recall is de winst).
2. **Detector-A-training (optie A):** generieke "keurmerk-regio"-detector voor strakke localisatie
   (proposer-B haalt 45 %@IoU0,5); bootstrap met B-proposals + 8.7-synthese-bboxen.
3. **Productie-build twee-traps-pad** als A/B-migratie náást template-match, per-klasse-cutover; GATED op (1).

**Beschermde tests** (`tests/test_artwork_processing.py` 142–206) pinnen `match_templates`/`tile_image`/
`merge_detections` → blijven byte-identiek; de region-proposer komt ernáást (A/B-migratie).

## Constraints (blijvend)

- Geen DB-migratie zonder expliciete per-geval toestemming (owner-pad: `ssh cherry`, `sudo -u postgres`).
- Containers herstart/deploy alleen na bevestiging.
- Geen Zoho binnen implement-sprint.
- Secrets nooit in code/logs. `gh` altijd als `GITHUB_TOKEN= GH_TOKEN= gh …`.
- Commits/PRs Engels + signature; versions.md bij user-zichtbare wijzigingen (12.x is nog dev-intern, geen versions.md).
- De 4 beschermde tests byte-identiek houden.

## Sprint-status

`_bmad-output/implementation-artifacts/sprint-status.yaml`: `epic-12: in-progress`,
`12-1-…: done`, `12-2-…: backlog` (spike loopt). Stories: `12-1-…md` (done), `12-2-…md` (ready-for-spike).
