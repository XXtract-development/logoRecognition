# AC → test-traceability — Story 15.4

Elk acceptatiecriterium is gedekt door minstens één geautomatiseerde test die het gedrag assert. 5 AC's, 5 gedekt.

| AC | Kern | Dekkende test(s) |
|----|------|------------------|
| **AC1** | Prisma-migratie `threshold_changes` (patroon `model_activation_logs`) ter expliciete goedkeuring (ARCH-2) | Migratie 0017 handmatig voorbereid + down.sql (`apps/api/prisma/migrations/0017_add_threshold_changes/`), lokaal toegepast op localhost:5432 na verificatie; model in `schema.prisma` volgt `ModelActivationLog` exact (uuid, VarChar, `@db.Timestamptz`, `@@index([changedAt(sort: Desc)])`, snake_case `@@map`/`@map`). Down-script = drop tabel. Additiviteit geverifieerd (`migrate status` up-to-date, geen drops). NB: de migratie-uitvoering is per teamregel een menselijke/toestemmingsactie; hier lokaal toegepast in de sandbox-DB. |
| **AC2** | Drempelbeheer: verplichte reden, log oud+nieuw+gebruiker+reden, historie zichtbaar, per-methode-drempels afzonderlijk | **API:** `flywheel-thresholds.test.ts` — "reden server-side verplicht" (lege + whitespace → `ThresholdReasonRequiredError`, geen audit-rij), "logt oude+nieuwe waarde+userId+reden in threshold_changes", "per-methode-view toont alle drie afzonderlijk met bron", "historie nieuwste boven", "override ?? env ?? default", bereik-validatie. `flywheel-thresholds-pause.routes.test.ts` — PUT 400 zonder reden, 400 buiten bereik, 400 onbekende methode, happy path geeft userId door, ADMIN-only; GET per-methode-view + historie. **Web:** `ThresholdModal.test.tsx` — per-methode-drempels afzonderlijk, opslaan disabled zonder reden, vaste audittrail-hint, wijzigingshistorie zichtbaar, opslaan roept `changeThreshold(methode, waarde, reden)` aan. |
| **AC3** | Pauzeknop → bevestigingsmodal → persistente amber pauzebanner; hervatten expliciet | **API:** `flywheel-pause-control.test.ts` — pauze persisteert `paused=true` + logt overgang (false→true) met gebruiker. `flywheel-thresholds-pause.routes.test.ts` — POST pause happy path + userId, 400 ongeldige actie, ADMIN-only. **Web:** `PauseSwitch.test.tsx` — omzetten opent bevestigingsmodal met consequentie-tekst, bevestigen roept `pauseFlywheel` aan. `StandstillBanner.test.tsx` — amber pauzebanner met `role="alert"` + wie/wanneer bij handmatige pauze. |
| **AC4** | Hervatting gelogd met gebruiker+tijd; hervat-modal toont openstaande quarantaines als waarschuwing zonder te blokkeren | **API:** `flywheel-pause-control.test.ts` — hervat logt overgang (true→false) met gebruiker; "BLOKKEERT NIET op openstaande quarantaines maar geeft het aantal als waarschuwing" (paused=false, openQuarantines=2). `flywheel-thresholds-pause.routes.test.ts` — POST resume happy path met openstaande-quarantaine-waarschuwing. **Web:** `PauseSwitch.test.tsx` — hervat-modal toont `resume-quarantine-warning` zonder de hervat-knop te blokkeren, bevestigen roept `resumeFlywheel` aan. |
| **AC5** | Automatische stilstand (K=2) → rode banner met aanleiding + links naar betrokken batches; notificatie via bestaand patroon zichtbaar | **API:** `flywheel-standstill.test.ts` — K=2 persisteert stilstand-record met beide batch-ids; `getStandstillPanel` → `mode: 'auto'` met `by: 'system'` + batchIds; bewuste hervatting wist de record (banner verdwijnt). De notificatie via het RetrainingNotification-patroon is 13.6-gedrag (`flywheel-pause.test.ts`, ongewijzigd) — 15.4 verifieert alleen zichtbaarheid, herbouwt niets (taak 5.3). **Web:** `StandstillBanner.test.tsx` — rode banner met `role="alert"`, aanleiding-tekst en batch-links naar `/flywheel/batches/:id`; geen rode banner bij handmatige pauze (kleursemantiek UX-DR5). |

## Regressiedekking (gewijzigde bestaande code)

De omzetting naar de async resolver raakt `nomination.ts`, `guardrails.ts`, `batch-detail.ts`. De bestaande suites dekken dat gedrag: `flywheel-nomination.test.ts` (15), `flywheel-guardrails.test.ts` (16), `flywheel-batch-detail.test.ts` (6), `flywheel-batch-detail.routes.test.ts` (14), `flywheel-config.test.ts` (12), `flywheel-overview-compose.test.ts` (2) — alle groen. De FlywheelPage-integratie (`FlywheelPage.test.tsx`, 15) blijft groen met de nieuwe header-controls en banners.
