---
scope: Referentie-vliegwiel zonder review (PRD 2026-07-02) — feature-slice op bestaande monorepo
purpose: Build substrate voor epics & stories
altitude: feature→epics
updated: 2026-07-02T22:25
---

- (decision) Paradigma: idempotente batch-pipeline met poortwachter (nominate→batch→gate→promote), gescheiden van de live-detectiestroom; geen event-sourcing, geen streaming — voorkomt dat units realtime-koppelingen op de detectie-flow bouwen
- (decision) Eigenaarschap: apps/api (Fastify+Prisma) is exclusieve eigenaar van alle vliegwiel-state in PostgreSQL; apps/ml-service is stateless compute (embeddings, gate, pHash, centroid/outlier, regressie-meting) en schrijft nooit vliegwiel-tabellen — voorkomt twee schrijvers op één entiteit
- (decision) Datamodel: ReferenceLogo blijft de enige bron van actieve referenties (gecureerd én gepromoveerd); NIEUWE tabellen reference_candidates (status candidate/promoted/rejected + evidence JSONB + herkomst crosscheck/kruischeck/bootstrap), promotion_batches (status pending/passed/quarantined/rolled-back + poort-uitkomsten JSONB + baseline-meting), hard_negatives (inhouds-hash uniek), gold_set_records (immutable, vervangings-verwijzing). Promotie = INSERT in ReferenceLogo met variantLabel-conventie auto-{batchShortId}-{seq} [ASSUMPTION] — voorkomt status-vlaggen verspreid over bestaande tabellen
- (constraint) Teamregel: DB-migraties alleen met expliciete toestemming per geval; alle schemawijzigingen gebundeld per story als Prisma-migratie die de gebruiker zelf goedkeurt/draait — stories moeten dit als taak opnemen, nooit auto-migrate
- (decision) Gold-set-opslag: PostgreSQL (gold_set_records) met eenmalige seed-import van de 91 bestaande JSON-samples + declaratie-goudstandaard (74 GTINs); repo-bestanden worden read-only legacy met verwijzing — lost PRD OQ-gold-set-opslag op
- (decision) Regressietest: schaduw-evaluatie in ml-service — kandidaat-embeddings in eigen tabel candidate_embeddings (pgvector, via API geschreven); eval bevraagt actieve ReferenceEmbedding UNION schaduwset zonder mutatie van de actieve set; meting per batch vastgelegd op promotion_batches [ASSUMPTION: precisie@drempel over volledige gold-set, tolerantie 1pp conform PRD]
- (decision) Orkestratie: promotielus als BullMQ repeatable job (nachtelijk) in de bestaande API-worker naast de pipeline-queue; ml-service-aanroepen via bestaande mlClient; GEEN nieuwe Coolify scheduled task — API bezit de state dus ook de orkestratie. Bootstrap-runs en outlier-audit als aparte BullMQ jobs met eigen cadans
- (constraint) ml-service Docker kopieert alleen app/ — alle nieuwe ml-service-code onder app/ (services/, geen scripts/) conform eerdere les (queue-harvester-incident)
- (decision) GLN-backfill: route (a) batch-export GTIN→GLN uit prod tradeItems (MongoDB read-only, off-peak, eenmalig) naar artwork_imports.gln + Redis-preload van declaraties; terugval (b) re-import via mediaserver-backfill voor het restant [ASSUMPTION: prod-read governance akkoord — expliciet af te stemmen vóór uitvoering; gemarkeerd als go/no-go in de betreffende story]
- (decision) 12.8-uitbreiding: crosscheck-worker emit nominaties (rij in reference_candidates) achter env-vlag FLYWHEEL_NOMINATION_ENABLED=false default; verdict-response-pad volledig ongewijzigd; zelfde vlag geldt voor kruischeck- én import-herkomst — voorkomt dat 12.8-implementatie en vliegwiel elkaars contract breken
- (decision) Dedup/outlier: alle beeld- en vectorberekeningen in ml-service (imagehash/pHash, pgvector-cosine, klasse-centroid); API roept aan en beslist op de geretourneerde scores; drempels als env-config met PRD-startwaarden — voorkomt gedupliceerde vectorlogica in Node
- (decision) Dashboard: bevestigd — antd 5 + ConfigProvider-theming met XXtract-tokens binnen bestaande SPA; route /flywheel; data via nieuwe read-endpoints /api/v1/flywheel/* (overview, batches/:id, thresholds, pause) — geen shadcn-migratie, geen aparte frontend
- (decision) Fail-closed invariant: elke poortstap die niet kan meten of bereiken blokkeert de batch (status quarantined met reden systeem-fout); pauzestand persistent in DB (system_settings-rij) zodat herstart de pauze niet opheft [ASSUMPTION]
- (decision) Idempotentie: nominatie-uniciteit op (inhouds-hash, t3777Code); batch-run herstart-veilig via batch-status-machine; kruischeck-herverwerking van zelfde GTIN levert geen duplicaat-nominaties — voorkomt dubbele promoties bij job-retries
- (decision) Herleidbaarheid: evidence JSONB per kandidaat (bron-GTIN, bronbestand, bbox, methode, scores, declaratie-uitkomst) + per batch (poort-uitkomsten, metingen, wie/wanneer bij handmatige acties); drempelwijzigingen in audit-log-tabel threshold_changes [ASSUMPTION: hergebruik bestaand audit-patroon indien aanwezig, anders eigen tabel]
- (question) Bestaat er al een system_settings/audit-patroon in apps/api om op aan te sluiten (Epic 10 audit-trail)? Distill-agent: verifieer in code en ratificeer bestaande conventie
- (direction) Deferred (bewust niet beslist): DINOv2-migratie, zero-shot region proposer, externe leverancier-terugkoppeling, automatische drempel-adaptatie, 39k-bulk-run-capaciteitsplanning
- (decision) AD-3 aangescherpt: promotie = één atomaire transactie (INSERT ReferenceLogo active=true source='flywheel-promotion' + kopie embedding-vector naar ReferenceEmbedding zonder herberekening + kandidaat promoted); rollback = active=false soft-delete, her-promotie met nieuw variantLabel — dicht adversarial F2 en rubric B-2
- (decision) AD-4 aangescherpt: actieve gold-set = uitsluitend records zonder opvolger (replacedById IS NULL), resolutie canoniek in apps/api services/flywheel/gold-set.ts; ml-service ontvangt geresolvede set als payload — dicht adversarial F7
- (decision) AD-5 aangescherpt: schaduwset = uitsluitend kandidaten met status in_batch in de batch-onder-meting (geen dubbeltelling); baseline = veld op de gepasseerde batch, rollback herstelt naar de laatst overgebleven gepasseerde batch — dicht adversarial F2/F6
- (decision) AD-6 aangescherpt: queue flywheel worker-concurrency 1; per-klasse cap ín de promotie-transactie afgedwongen (check+insert atomair); nieuwe repeatable jobs via BullMQ Job Schedulers (upsertJobScheduler, 5.63) — dicht adversarial F5 en reality-check F-2
- (decision) AD-11 aangescherpt: pauze blokkeert nominatie-inserts én batch-verwerking (promotion+bootstrap+hooks), outlier-audit en dashboard-reads draaien door; FR-19-stilstand-notificatie via bestaand RetrainingNotification-patroon + dashboard-banner — dicht adversarial F8 en rubric B-5
- (decision) AD-12 aangescherpt: inhouds-hash verwijst nu expliciet naar de canonieke ml-service-hash van AD-14 (bestaande opslagsleutel bron+bbox ongeschikt) — sluit de AD-9×AD-12-spanning uit adversarial F1
- (decision) AD-14 nieuw: één canonieke inhouds-hash-implementatie in ml-service /ml/phash (SHA-256 over pixel-buffer na gepinde normalisatie); API berekent nooit zelf; synchroon bij nominatie; hash-service onbereikbaar = nominatie geweigerd (fail-closed) — dicht adversarial F1
- (decision) AD-15 nieuw: uitsluitend de flywheel-worker instantieert promotiebatches; endpoints muteren alleen kandidaat-status en enqueue-en werk; claim-semantiek via promotionBatchId (kandidaat in hoogstens één niet-afgesloten batch) — dicht adversarial F3
- (decision) AD-16 nieuw: kandidaat-status-machine candidate→in_batch→promoted/rejected met conditional updates (WHERE status=verwacht); decision-endpoint 409 op kandidaten in batch in verwerking — dicht adversarial F4
- (decision) Sectie Operationele envelope toegevoegd: ACC deployt pre-built ghcr-images (wachten op GitHub Actions-workflow), migraties handmatig per release na toestemming, seeds idempotent+droge-run handmatig, flywheel-job-falen notificeert via RetrainingNotification+banner; plus mismatch_events uit Deferred naar Structural Seed, GS1-gidsbeelden-conventie, Stack naar lockfile-versies (BullMQ 5.63.0, Fastify 4.29.1, Prisma 5.22.0, ioredis 5.8.2, TS 5.7.2, ImageHash==4.3.2) — dicht rubric B-1/B-3/B-4 en reality-check F-1/F-3
- (event) spine finalized — reviewer gate verwerkt (adversarial F1-F8, reality-check, rubric operationele envelope)
